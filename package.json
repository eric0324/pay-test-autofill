{
  "name": "pay-test-autofill",
  "version": "0.1.0",
  "description": "開發者金流測試卡一鍵自動填入外掛（藍新／綠界／Stripe），支援 Chrome 與 Firefox。",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build.mjs",
    "package:firefox": "bun run build && bunx web-ext build --source-dir dist/firefox --artifacts-dir dist --overwrite-dest",
    "package:chrome": "bun run build && bunx web-ext build --source-dir dist/chrome --artifacts-dir dist --overwrite-dest --filename pay-test-autofill-chrome-0.1.0.zip",
    "lint:firefox": "bun run build && bunx web-ext lint --source-dir dist/firefox",
    "demo": "bun demo/server.mjs",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.10.6"
  }
}
